name: Run Real Tests in CI

on: workflow_dispatch

jobs:
  Run_CI:
   runs-on: ubuntu-latest
   steps:
     - name: code check
       uses: actions/checkout@v5

  test:
   runs-on: ubuntu-latest
   steps:
     - name: code verify
       uses:  actions/checkout@v5

     - name: run the shell script
       run: |
        echo "My secript is running"
        chmod +x newshellscrit.sh
        ./newshellscrit.sh
