name: ARTIFICATE

on: workflow_dispatch

jobs:
  artifact:
   runs-on: ubuntu-latest
   steps:
     - name: code quality
       uses: actions/checkout@v5
       
     - name: cretea a file
       run: echo "this is suraj" > output.txt

     - name: upload the artefact file
       uses: actions/upload-artifcat@v4
       with:
         name: my-artifcate
         path: output.txt
