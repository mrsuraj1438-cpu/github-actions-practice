name: Verify Secret

on: workflow_dispatch

jobs:
  test_secret:
   runs-on: ubuntu-latest
   steps:
   
     - name: code check
       uses: actions/checkout@v5
       env:
         PASSWORD: ${{secrets.MY_SECRET_MESSAGE}}

     - name: SECRETS verify
       run: echo "$PASSWORD"
        
